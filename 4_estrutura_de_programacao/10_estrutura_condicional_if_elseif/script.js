let nome = "Mariana";

if (nome != undefined && nome === "Maia") {
  console.log("Nome definido");
} else if (nome === "Maria" && nome.length === 5) {
  console.log("Mesmo nome e com a quantidade de caracteres compatível.");
} else {
  console.log("Nome não atende aos requisitos.");
}
